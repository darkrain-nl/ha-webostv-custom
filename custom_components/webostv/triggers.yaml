turn_on:
  fields:
    entity_id:
      selector:
        entity:
          integration: webostv
          domain: media_player
          multiple: true
    device_id:
      selector:
        device:
          integration: webostv
          multiple: true
